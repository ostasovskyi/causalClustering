% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{search_causal_clustering}
\alias{search_causal_clustering}
\title{Search causal clustering over K for fixed xi}
\usage{
search_causal_clustering(
  W,
  xi = 0,
  min_k = 2,
  max_k = floor(nrow(W)/2),
  n_eig = NULL,
  seed = NULL,
  engine = c("sdp", "spectral"),
  methods = available_discretization_methods(),
  include_bernoulli = FALSE,
  objective_type = "squared",
  k_constraint = FALSE,
  gamma_bar = 10,
  box_constraints = TRUE,
  try_sign_flip = TRUE,
  keep_sdp_solutions = FALSE
)
}
\arguments{
\item{W}{Symmetric binary adjacency matrix.}

\item{xi}{Non-negative tuning parameter.}

\item{min_k}{Minimum K.}

\item{max_k}{Maximum K.}

\item{n_eig}{Optional number of eigenvectors used for rounding.}

\item{seed}{Optional k-means seed.}

\item{engine}{\code{"sdp"} is the SDP relaxation; \code{"spectral"} is a heuristic.}

\item{methods}{Character vector of rounding/discretization methods. All requested methods are run and the rounded candidate with the smallest realized objective is selected.}

\item{include_bernoulli}{Whether to include the all-singleton design.}

\item{objective_type}{Must be \code{"squared"} for Equation (9).}

\item{k_constraint}{Boolean from Algorithm 1.}

\item{gamma_bar}{Cluster-size constant for optional constraints.}

\item{box_constraints}{Whether to impose \verb{0 <= X_ij <= 1} in the K-specific SDP.}

\item{try_sign_flip}{Whether to try rounding both X and -X. This is useful when comparing multiple discretizations after a relaxation step.}

\item{keep_sdp_solutions}{Whether to return full SDP objects.}
}
\value{
List containing the selected clustering, candidate objectives, SDP
lower bounds, and \code{Gamma_n} when available.
}
\description{
Implements Algorithm 1 for a fixed value of \code{xi}. With
\code{k_constraint = FALSE}, the relaxation is solved once and rounded over the
K grid. With \code{k_constraint = TRUE}, a separate K-specific SDP with optional
constraints is solved for every K.
}
