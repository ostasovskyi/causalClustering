% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{causal_clustering_algorithm1}
\alias{causal_clustering_algorithm1}
\title{Algorithm 1: causal clustering for one calibration value}
\usage{
causal_clustering_algorithm1(
  W,
  xi = NULL,
  min_k = 2,
  max_k = floor(nrow(W)/2),
  n_eig = NULL,
  seed = NULL,
  engine = c("sdp", "spectral"),
  methods = available_discretization_methods(),
  include_bernoulli = FALSE,
  objective_type = "squared",
  k_constraint = FALSE,
  gamma_bar = 10,
  box_constraints = TRUE,
  try_sign_flip = TRUE,
  keep_search_matrices = FALSE,
  keep_sdp_solutions = FALSE,
  xi_grid = NULL,
  calibration = NULL
)
}
\arguments{
\item{W}{Symmetric binary adjacency matrix.}

\item{xi}{Non-negative Algorithm 1 calibration, equal to
\code{(lambda * phibar_n^2 / psibar)^(-1)} in the paper.}

\item{min_k}{Minimum number of clusters to consider.}

\item{max_k}{Maximum number of clusters to consider.}

\item{n_eig}{Optional number of eigenvectors used for rounding. If \code{NULL},
the number of eigenvectors equals the candidate K.}

\item{seed}{Optional seed used by k-means rounding and randomized baselines.}

\item{engine}{Optimization engine. Use \code{"sdp"} for the semidefinite relaxation and
\code{"spectral"} for a spectral heuristic.}

\item{methods}{Character vector of rounding/discretization methods. See
\code{\link[=available_discretization_methods]{available_discretization_methods()}}. All requested methods are run and the rounded candidate with the smallest realized objective is selected.}

\item{include_bernoulli}{Logical; if \code{TRUE}, also compare against the
all-singleton Bernoulli design. This option is outside the approximation
certificate in Algorithm 1.}

\item{objective_type}{Must be \code{"squared"} for Equation (9).}

\item{k_constraint}{Logical Boolean from Algorithm 1.}

\item{gamma_bar}{Upper cluster-size multiplier used by the optional
K-specific constraints.}

\item{box_constraints}{Logical; whether to impose the optional constraints
\verb{0 <= X_ij <= 1} when \code{k_constraint = TRUE}.}

\item{try_sign_flip}{Logical; if \code{TRUE}, round both the relaxation matrix
and its negative.}

\item{keep_search_matrices}{Logical; if \code{TRUE}, keep the relaxation matrix in
the returned object.}

\item{keep_sdp_solutions}{Logical; if \code{TRUE}, keep raw SDP solution objects.}

\item{xi_grid}{Optional scalar alias for \code{xi}. Use
\code{\link[=causal_clustering_algorithm2]{causal_clustering_algorithm2()}} for calibration ranges.}

\item{calibration}{Optional positive scalar equal to
\code{lambda * phibar_n^2 / psibar}; converted to \code{xi = 1 / calibration}.}
}
\value{
A list containing the selected \code{clusters}, selected K, objective
value, objective components, SDP lower bounds, and the approximation
certificate \code{Gamma_n} when available.
}
\description{
Implements Algorithm 1 for a single value of \code{xi}. The \code{k_constraint}
argument implements the Boolean branch in Algorithm 1: \code{FALSE} solves one relaxation and rounds over
all K values, while \code{TRUE} solves a K-specific SDP relaxation for every K.
}
