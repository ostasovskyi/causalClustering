% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{run_single_network}
\alias{run_single_network}
\title{Run one simulated network instance}
\usage{
run_single_network(
  seed,
  parameters_graph = list(type_graph = "geometric", n = 100, neighb = 2),
  W = NULL,
  xi_seq = seq(from = 0.1, to = 10, by = 1),
  min_k = 2,
  max_k = NULL,
  include_louvain = FALSE,
  engine = c("sdp", "spectral"),
  methods = c("kmeans"),
  objective_type = "squared",
  k_constraint = FALSE,
  gamma_bar = 10,
  box_constraints = TRUE
)
}
\arguments{
\item{seed}{Random seed.}

\item{parameters_graph}{Graph-generation parameters.}

\item{W}{Optional adjacency matrix. If \code{NULL}, a network is simulated.}

\item{xi_seq}{Sequence of xi values.}

\item{min_k}{Minimum number of clusters in the causal search.}

\item{max_k}{Maximum number of clusters in the causal search.}

\item{include_louvain}{Logical; whether to compute the Louvain baseline.}

\item{engine}{Search engine for the causal clustering step. Use \code{"sdp"} for
the semidefinite relaxation and \code{"spectral"} for a spectral heuristic.}

\item{methods}{Discretization methods used after the relaxation step.}

\item{objective_type}{Must be \code{"squared"}.}

\item{k_constraint}{Logical; passed to \code{search_causal_clustering()}.}

\item{gamma_bar}{Upper cluster-size multiplier used when \code{k_constraint = TRUE}.}

\item{box_constraints}{Logical; whether to include optional box constraints
when \code{k_constraint = TRUE}.}
}
\value{
A list with \code{results}, \code{W}, and \code{clusters}.
}
\description{
Run one simulated network instance
}
