% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{causal_clustering_algorithm}
\alias{causal_clustering_algorithm}
\title{Causal clustering algorithm}
\usage{
causal_clustering_algorithm(
  W,
  xi = NULL,
  calibration = NULL,
  xi_grid = NULL,
  xi_range = NULL,
  calibration_grid = NULL,
  calibration_range = NULL,
  min_k = 2,
  max_k = floor(nrow(W)/2),
  n_eig = NULL,
  seed = NULL,
  engine = c("sdp", "spectral"),
  methods = available_discretization_methods(),
  include_bernoulli = FALSE,
  objective_type = "squared",
  k_constraint = FALSE,
  gamma_bar = 10,
  box_constraints = TRUE,
  try_sign_flip = TRUE,
  keep_search_matrices = FALSE,
  keep_sdp_solutions = FALSE
)
}
\arguments{
\item{W}{Symmetric binary adjacency matrix.}

\item{xi}{Optional scalar or grid of \code{xi = (lambda * phibar_n^2 / psibar)^(-1)} values.}

\item{calibration}{Optional scalar or grid of \code{lambda * phibar_n^2 / psibar} values.}

\item{xi_grid}{Optional positive grid of \code{xi} values.}

\item{xi_range}{Optional length-two positive range of \code{xi} values.}

\item{calibration_grid}{Optional positive grid of \code{lambda * phibar_n^2 / psibar} values.}

\item{calibration_range}{Optional length-two positive range of \code{lambda * phibar_n^2 / psibar} values.}

\item{min_k}{Minimum number of clusters to consider.}

\item{max_k}{Maximum number of clusters to consider.}

\item{n_eig}{Optional number of eigenvectors used for rounding. If \code{NULL},
the number of eigenvectors equals the candidate K.}

\item{seed}{Optional seed used by k-means rounding.}

\item{engine}{Optimization engine. Use \code{"sdp"} for the semidefinite relaxation and
\code{"spectral"} for a spectral heuristic. Calibration grids/ranges require \code{"sdp"}.}

\item{methods}{Character vector of rounding/discretization methods. See
\code{\link[=available_discretization_methods]{available_discretization_methods()}}. All requested methods are run and
the rounded candidate with the smallest realized objective is selected.}

\item{include_bernoulli}{Logical; if \code{TRUE}, also compare against the
all-singleton Bernoulli design.}

\item{objective_type}{Must be \code{"squared"}.}

\item{k_constraint}{Logical Boolean from Algorithm 1. For Algorithm 2 it
filters rounded candidates by the cluster-size constraint.}

\item{gamma_bar}{Upper cluster-size multiplier used by optional constraints or filtering.}

\item{box_constraints}{Logical; whether to impose optional \verb{0 <= X_ij <= 1}
constraints when Algorithm 1 is run with \code{k_constraint = TRUE}.}

\item{try_sign_flip}{Whether to try rounding both X and -X.}

\item{keep_search_matrices}{Logical; if \code{TRUE}, keep the relaxation matrix in
Algorithm 1 results.}

\item{keep_sdp_solutions}{Logical; if \code{TRUE}, keep raw SDP solution objects.}
}
\value{
A list. For scalar calibrations it contains the Algorithm 1 output,
including \code{clusters}, \code{selected_k}, \code{selected_method}, \code{objective},
\code{components}, SDP lower bounds, and \code{Gamma_n} when available. For grids or
ranges it contains the Algorithm 2 output, including \code{clusters},
\code{selected_k}, \code{selected_method}, endpoint lower bounds, endpoint regret
values, and objective components.
}
\description{
Unified interface for the causal clustering algorithms. Supply exactly one
calibration input. A scalar \code{xi} or scalar \code{calibration} calls Algorithm 1
for a fixed calibration. A grid or range of \code{xi} or \code{calibration} values
calls Algorithm 2 and uses only the endpoint range for the endpoint-regret
criterion.
}
