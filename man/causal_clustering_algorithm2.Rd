% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{causal_clustering_algorithm2}
\alias{causal_clustering_algorithm2}
\title{Algorithm 2: causal clustering over a range of spillover calibrations}
\usage{
causal_clustering_algorithm2(
  W,
  xi_lower = NULL,
  xi_upper = NULL,
  xi_range = NULL,
  calibration_range = NULL,
  min_k = 2,
  max_k = floor(nrow(W)/2),
  n_eig = NULL,
  seed = NULL,
  engine = "sdp",
  methods = available_discretization_methods(),
  include_bernoulli = FALSE,
  k_constraint = FALSE,
  gamma_bar = 10,
  try_sign_flip = TRUE,
  keep_sdp_solutions = FALSE
)
}
\arguments{
\item{W}{Symmetric binary adjacency matrix.}

\item{xi_lower}{Positive lower endpoint for xi.}

\item{xi_upper}{Positive upper endpoint for xi.}

\item{xi_range}{Optional length-2 positive range for xi.}

\item{calibration_range}{Optional length-2 range for
\code{lambda * phibar_n^2 / psibar}; converted internally to \code{xi = 1/calibration}.}

\item{min_k}{Minimum K.}

\item{max_k}{Maximum K.}

\item{n_eig}{Optional number of eigenvectors used for rounding.}

\item{seed}{Optional k-means seed.}

\item{engine}{Must be \code{"sdp"} for Algorithm 2.}

\item{methods}{Character vector of rounding/discretization methods. All requested methods are run and the rounded candidate with the smallest realized objective is selected.}

\item{include_bernoulli}{Whether to include the all-singleton design.}

\item{k_constraint}{Whether to filter rounded candidates by the size constraint.}

\item{gamma_bar}{Cluster-size constant for optional filtering.}

\item{try_sign_flip}{Whether to try rounding both X and -X. This is useful when comparing multiple discretizations after a relaxation step.}

\item{keep_sdp_solutions}{Whether to return full SDP solution objects.}
}
\value{
List containing the selected clustering, endpoint lower bounds, and
endpoint regret values.
}
\description{
Implements Algorithm 2. It solves endpoint SDP lower
bounds at the two endpoints of the xi range, solves the endpoint-regret SDP,
rounds the resulting matrix for each K, and selects the clustering with the
smallest endpoint regret.
}
