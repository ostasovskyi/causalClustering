#' causalClust: causal clustering for network experiments
#'
#' Tools for constructing and evaluating cluster randomized designs under
#' network interference. The main design functions are
#' [causal_clustering_algorithm1()] and [causal_clustering_algorithm2()].
#'
#' @keywords internal
"_PACKAGE"

if (getRversion() >= "2.15.1") {
  utils::globalVariables(c(".data"))
}
