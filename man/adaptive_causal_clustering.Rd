% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{adaptive_causal_clustering}
\alias{adaptive_causal_clustering}
\title{Adaptive causal clustering}
\usage{
adaptive_causal_clustering(
  W,
  calibration_grid = NULL,
  xi_grid = NULL,
  xi_range = NULL,
  calibration_range = NULL,
  min_k = 2,
  max_k = floor(nrow(W)/2),
  n_eig = NULL,
  seed = NULL,
  engine = "sdp",
  methods = c("kmeans"),
  include_bernoulli = FALSE,
  objective_type = "squared",
  k_constraint = FALSE,
  gamma_bar = 10,
  try_sign_flip = FALSE,
  keep_sdp_solutions = FALSE
)
}
\arguments{
\item{W}{Symmetric binary adjacency matrix.}

\item{calibration_grid}{Optional positive grid for
\code{lambda * phibar_n^2 / psibar}. The endpoint range of \code{1 / calibration_grid}
is used.}

\item{xi_grid}{Optional positive grid of \code{xi} values. The endpoint range
is used.}

\item{xi_range}{Optional length-two positive range for \code{xi}.}

\item{calibration_range}{Optional length-two positive range for
\code{lambda * phibar_n^2 / psibar}; converted to a \code{xi} range.}

\item{min_k}{Minimum number of clusters to consider.}

\item{max_k}{Maximum number of clusters to consider.}

\item{n_eig}{Optional number of eigenvectors used for rounding.}

\item{seed}{Optional seed used by k-means rounding.}

\item{engine}{Must be \code{"sdp"} for Algorithm 2.}

\item{methods}{Character vector of rounding/discretization methods.}

\item{include_bernoulli}{Logical; if \code{TRUE}, also compare the all-singleton
Bernoulli design.}

\item{objective_type}{Must be \code{"squared"} for Algorithm 2.}

\item{k_constraint}{Logical; if \code{TRUE}, rounded candidates are filtered by
the cluster-size constraint.}

\item{gamma_bar}{Upper cluster-size multiplier used when \code{k_constraint = TRUE}.}

\item{try_sign_flip}{Logical; if \code{TRUE}, round both the relaxation matrix and
its negative.}

\item{keep_sdp_solutions}{Logical; if \code{TRUE}, keep raw SDP solution objects.}
}
\value{
The list returned by \code{\link[=causal_clustering_algorithm2]{causal_clustering_algorithm2()}}.
}
\description{
Public wrapper for Algorithm 2. Supply either \code{xi_range},
\code{xi_grid}, \code{calibration_range}, or \code{calibration_grid}; the function converts
the input to an endpoint range and calls \code{\link[=causal_clustering_algorithm2]{causal_clustering_algorithm2()}}.
}
